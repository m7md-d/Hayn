# CLAUDE.md — دليل الوكيل الرئيسي

> هذا الملف هو نقطة الدخول لأي وكيل (أو مطوّر) يشتغل على المشروع. اقرأه بالكامل قبل أي كود.
> اسم المشروع: **Hayn**. الـ Dart package name: `hayn`.

---

## 1. ما هو المشروع باختصار

تطبيق **Flutter** لإدارة ومعالجة الميديا (صور / صوت / فيديو) يعمل **أوفلاين بالكامل** بلا أي اتصال إنترنت. مجاني، والربح عبر **التبرعات الاختيارية** فقط (لا إعلانات، لا اشتراكات، لا تتبّع).

الميزات الأساسية:
1. ضغط الصور وتحويل صيغها (مع شجرة صيغ ذكية: AVIF → HEIC → WebP/JPEG).
2. قص الصور + حذف الميتاداتا.
3. **الميزة الجراحية**: استبدال الصورة في الجهاز بنسخة مضغوطة مع الحفاظ على الاسم والتواريخ والميتاداتا وترتيب الألبوم (أخطر ميزة — اقرأ §3).
4. تجميع صور في صورة متحركة، وتحويل فيديو لصورة متحركة، واستخراج فريمات (GIF / WebP / AVIF متحرك).
5. تحرير فيديو: قص لاتلف (lossless trim) + قص دقيق (smart cut) + قص حواف (crop) + ضغط وتحكّم بالصيغ.
6. حذف الصوت من الفيديو (نظيف 100%).
7. **إزالة الموسيقى/الآلات مع إبقاء الكلام** (فصل مصادر صوتية أوفلاين عبر موديل AI — أثقل ميزة).

تفصيل كل ميزة في `docs/features/`. النطاق الكامل في `docs/01-PRD.md`.

---

## 2. القواعد الذهبية (Non-negotiable)

اقرأها كأنها دستور. أي مخالفة تُعتبر خطأ جسيم.

1. **لا إنترنت. أبدًا.** ممنوع أي `http`/`socket`/تحليلات/تتبّع. كل معالجة على الجهاز. لا تضف أي package يتصل بالشبكة. التطبيق يجب أن يعمل في وضع الطيران بالكامل.

2. **لا تلف الصوت/الصورة (No quality damage).** القاعدة العامة: **لا تعيد الترميز (re-encode) إلا للضرورة القصوى**، وإذا اضطررت فمرّة واحدة فقط وبإعدادات جودة عالية. التشفير المتكرّر = تشويه تراكمي ممنوع.

3. **سلامة بيانات المستخدم فوق كل شيء.** أي عملية تلمس ملفات المستخدم الأصلية (خصوصًا الجراحية) تُبنى كـ **معاملة آمنة (safe transaction)**: لا حذف للأصل إلا بعد إثبات سلامة البديل + وجود نسخة احتياطية / سلة محذوفات قابلة للتراجع. تفاصيل في §3.

4. **العمل الثقيل خارج الـ main isolate.** أي ترميز/فك ترميز/معالجة بكسلات/فصل صوت يجري في **Isolate منفصل** أو في الكود الأصلي (native). الواجهة يجب ألا تتجمّد أبدًا (هدف ثابت 60/120fps).

5. **احترم الرخص.** تجنّب مكوّنات GPL في الـ binary النهائي قدر الإمكان (المشروع يطمح للنشر بترخيص متساهل). فضّل المُشفّرات الخالية من الإتاوات (AV1/AVIF, VP9/WebP) ومُشفّرات النظام. لا تبنِ على HEVC/x265 داخل الـ binary. التفاصيل في `docs/03-FORMATS.md`.

6. **ثنائية اللغة وثنائية الثيم منذ اليوم الأول.** كل نص عبر طبقة الترجمة (عربي/إنجليزي)، كل واجهة تدعم RTL/LTR، وكل لون عبر الثيم (داكن/فاتح). ممنوع نص أو لون مكتوب hard-coded في الـ widgets.

7. **لا افتراضات صامتة.** عند الشك في قدرة جهاز (هل يشفّر HEIC بالعتاد؟ هل يدعم AVIF؟) → **اكشف القدرة فعليًا** ثم قرّر، لا تفترض.

---

## 3. القواعد الحرجة للميزة الجراحية (اقرأ مرتين)

الميزة الجراحية تحذف/تستبدل صورًا لا تُعوّض. خطأ واحد = فقدان ذكرى للمستخدم. القواعد:

- **ترتيب العملية إلزامي:** (1) شفّر لملف مؤقت → (2) تحقّق من سلامته فعليًا (يُفكّ، الأبعاد مطابقة، غير معطوب، وأصغر فعلًا من الأصل) → (3) انسخ كل الميتاداتا والتواريخ والـ GPS من الأصل للنسخة → (4) **بعدها فقط** استبدل → (5) احتفظ بنسخة احتياطية في سلة محذوفات قابلة للتراجع لمدة قابلة للضبط.
- **never delete-before-verify.** لا يُحذف الأصل قبل أن يُثبت البديل صلاحيته.
- **idempotent & crash-safe.** لو انقطع التطبيق في منتصف العملية، يجب أن تبقى الصورة الأصلية سليمة أو قابلة للاستعادة. استخدم journal/log للعمليات.
- **ساحة المقارنة (Comparison Arena) قبل الاستبدال:** يرى المستخدم الأصل مقابل البديل (تكبير، فرق الحجم، الميتاداتا المحفوظة) ويؤكد قبل أي كتابة.
- اختلاف المنصّتين جوهري (Android تكتب فوق البايتات؛ iOS محدودة بـ PhotoKit). التفاصيل الكاملة في `docs/features/F2-surgical-replace.md`. **لا تكتب كود الجراحية قبل قراءته بالكامل.**

---

## 4. الـ Stack والحزم الأساسية

| الطبقة | الأداة | ملاحظة |
|---|---|---|
| إطار العمل | Flutter (أحدث مستقر) + Dart | — |
| إدارة الحالة | Riverpod | يفضّل على setState للمنطق المعقّد |
| تصفّح ميديا الجهاز | `photo_manager` | يقرأ الألبومات/الأصول أوفلاين |
| الصلاحيات | `permission_handler` | Scoped Storage (Android) / PhotoKit (iOS) |
| قص الصور | `image_cropper` | واجهة native جاهزة |
| ضغط الصور | `flutter_image_compress` | native، يدعم HEIC على iOS |
| AVIF (عرض + تشفير) | `flutter_avif` | libavif/aom — royalty-free |
| معالجة EXIF/بكسلات بدارت | `image` | بطيء على الكبير — للاستخدام المحدود فقط |
| فيديو/صوت (FFmpeg) | فورك مجتمعي حيّ: `ffmpeg_kit_flutter_new` | الأصلية ماتت (انظر §6) |
| ضغط فيديو سريع | المُشفّر الأصلي (MediaCodec/VideoToolbox) عبر platform channel | أسرع + يتفادى GPL |
| فصل المصادر الصوتية | ONNX Runtime (`onnxruntime`) + موديل HTDemucs (MIT) | في isolate، آخر مرحلة |
| المهام الطويلة | Dart Isolates + Android Foreground Service | إلزامي للترميز الطويل |
| التخزين المحلي | `drift` أو `isar` (DB) + ملفات | فهرسة المصغّرات والمهام |

التفاصيل والمبررات في `docs/02-ARCHITECTURE.md`.

---

## 5. بنية المجلدات المقترحة

```
lib/
  app/                 # bootstrap, theme, localization, routing
  core/                # utilities, result types, device-capability detection, isolates infra
  data/                # repositories, DB, file access, native channels
  features/
    library/           # تصفّح الميديا
    image_ops/         # ضغط/قص/حذف ميتاداتا
    surgical/          # الميزة الجراحية (+ comparison arena + trash)
    animated/          # الصور المتحركة
    video_edit/        # تحرير الفيديو
    audio/             # حذف الصوت + فصل المصادر
    settings/          # الإعدادات
  shared/              # widgets, design system, components
native/
  android/            # MediaStore writer, MediaCodec, Foreground Service, HeifWriter
  ios/                # PhotoKit, VideoToolbox, ImageIO/HEIC
assets/
  models/             # موديل الفصل الصوتي (ONNX) — مضمّن
  i18n/               # ملفات الترجمة ar/en
```

---

## 6. تحذير الـ FFmpeg (مهم — لا تتجاهله)

مكتبة `ffmpeg_kit_flutter` الأصلية من Arthenica **متقاعدة منذ يناير 2025 وحُذفت binaries-ها من المستودعات في أبريل 2025**. أي tutorial قديم سيوجّهك لمكتبة ميتة.

- استخدم فورك مجتمعي حيّ: **`ffmpeg_kit_flutter_new`** (نفس الـ Dart API القديم، فالأوامر نفسها تنفع).
- ثبّت نسخة الفورك بدقّة في `pubspec.yaml` ووثّق رقمها.
- اختر variant **LGPL** (وليس `full_gpl`) للحفاظ على مرونة الترخيص؛ هذا يستثني x264/x265 — وهو مقصود (نستخدم المُشفّر الأصلي للفيديو بدلًا منها).

---

## 7. أعراف الكود (Conventions)

- **لا تلف صامت للأخطاء.** استخدم نوع `Result<T, E>` (أو `fpdart`) للعمليات التي قد تفشل (ترميز، وصول ملفات). لا `try/catch` يبلع الخطأ.
- **كل عملية ميديا = task قابلة للإلغاء** مع تقدّم (`Stream<Progress>`). انظر طابور المهام في `02-ARCHITECTURE.md`.
- **لا I/O على الـ main isolate.** قراءة/كتابة الملفات الكبيرة في isolate.
- **اختبر على ملفات حقيقية متنوعة:** HEIC من iPhone، فيديو HDR، صور بشفافية (PNG)، Live Photos، فيديوهات بمعدّل إطارات متغيّر (VFR).
- **التزم بشجرة القرار للصيغ** في `03-FORMATS.md` ولا تخترع صيغة افتراضية.
- علّق بالإنجليزية في الكود؛ الوثائق بالعربية + مصطلحات إنجليزية.

---

## 8. كيف تشتغل على المشروع

1. اقرأ هذا الملف + `01-PRD.md` + `02-ARCHITECTURE.md` + `03-FORMATS.md`.
2. حدّد المرحلة الحالية من `05-ROADMAP.md`.
3. قبل تنفيذ ميزة، اقرأ ملفها في `docs/features/`.
4. اكتب الميزة + اختباراتها معًا. لا تُعلن إكمال مرحلة قبل اجتياز **معايير الخروج (exit criteria)** و**حالات الاختبار** في `06-TESTING.md`.
5. الميزة الجراحية وفصل الصوت: لا تبدأهما إلا بعد استقرار البنية والميزات الأخف.

> **مبدأ حاكم:** البطء المقبول + النظافة المضمونة + السلامة > السرعة + المخاطرة. عند أي تعارض، اختر الأأمن.
